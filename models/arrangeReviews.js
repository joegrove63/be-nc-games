const db = require('../db/connection');

const arrangeReviews = () => {
  // console.log('in the model!');
  //   return db.query(`SELECT * FROM reviews;`).then((categories) => {
  //   return categories.rows;
  //   });
};

module.exports = arrangeReviews;
